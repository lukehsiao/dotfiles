themes {
  omarchy {
    bg "{{ selection }}"
    fg "{{ foreground }}"
    black "{{ background }}"
    red "{{ color1 }}"
    green "{{ color2 }}"
    yellow "{{ color3 }}"
    blue "{{ color4 }}"
    magenta "{{ color5 }}"
    cyan "{{ color6 }}"
    white "{{ foreground }}"
    orange "{{ color3 }}"
  }
}
