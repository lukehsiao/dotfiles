{{- $email := promptString "email" -}}
{{- $name := promptString "name" -}}
[data]
    email = "{{ $email }}"
    name = "{{ $name }}"

[edit]
    command = "nvim"

[merge]
    command = "nvim"
    args = ["-d", "{{`{{ .Destination }}`}}", "{{`{{ .Source }}`}}", "{{`{{ .Target }}`}}"]
