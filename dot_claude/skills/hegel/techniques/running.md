# Case counts

Run with high case counts — thousands per property, not the library default; check the hegel docs for how counts and time limits are configured. High counts are cheap on small generators and are what reach rare paths. If the suite gets slow, raise counts selectively on the properties with the richest input spaces rather than lowering them everywhere.
